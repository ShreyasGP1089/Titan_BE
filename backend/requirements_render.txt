# Render Backend Requirements (Thin API Gateway)
# For 512 MB RAM free tier - NO ML MODELS

# Database
psycopg2-binary==2.9.9

# Database ORM
SQLAlchemy==2.0.23

# API Framework
flask==3.0.0
flask-cors==4.0.0
flask-restx==1.3.0

# HTTP Client (for calling local model server)
requests==2.31.0

# Utilities
python-dotenv==1.0.0

# pgvector support
pgvector==0.2.4

# Minimal numpy (for array handling only)
numpy==1.24.3

# NO torch
# NO transformers  
# NO sentence-transformers
# NO accelerate
# NO peft
# NO bitsandbytes

# All ML models run on local server (Mac) via HTTP
