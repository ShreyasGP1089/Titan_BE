# Production Environment Configuration
# Copy to .env and update with your actual values

# Database Configuration
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=decathlon_db
POSTGRES_USER=decathlonuser
POSTGRES_PASSWORD=change_this_secure_password

# API Authentication
API_KEY=change_this_to_a_secure_random_key

# Model Configuration
# Set to 'true' for production Linux servers
# Set to 'false' for local Mac development with MLX
USE_HF_PLANNER=true

# Hugging Face Cache (optional, improves startup time)
HF_HOME=/app/.cache/huggingface
TRANSFORMERS_CACHE=/app/.cache/transformers

# Flask Configuration
FLASK_ENV=production
FLASK_DEBUG=false

# Optional: Logging
LOG_LEVEL=INFO
LOG_FILE=/app/logs/api.log
