# Production Requirements for Linux Deployment
# Removed MLX and Apple Silicon dependencies

# Core Web Framework
flask==3.0.0
flask-cors==4.0.0
flask-restx==1.3.0

# Database
psycopg2-binary==2.9.9

# ML/NLP - Hugging Face Stack
transformers>=4.51.0
torch>=2.2.0
sentence-transformers==2.7.0
peft>=0.10.0  # For LoRA adapter support
accelerate>=0.27.2
safetensors>=0.4.2
# bitsandbytes==0.43.0  # Only needed for GPU 8-bit quantization - commented out for CPU deployment

# Data handling
numpy==1.24.3
datasets==2.18.0

# Utilities
python-dotenv==1.0.0
gunicorn==21.2.0  # Production WSGI server

# Optional: FastAPI alternative (if you want to switch from Flask)
# fastapi==0.110.0
# uvicorn==0.29.0
