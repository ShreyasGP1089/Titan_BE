DEBUG ENDPOINTS - QUICK SUMMARY
================================

## ✅ IMPLEMENTATION COMPLETE

Added 5 debug endpoints to test components independently.

## 📍 New Endpoints

```
POST /api/v1/debug/parse-query        # Test SLM only
POST /api/v1/debug/search             # Test SearchTool
POST /api/v1/debug/task               # Test TaskTool + Budget Optimizer
POST /api/v1/debug/compare            # Test CompareTool
POST /api/v1/debug/alternatives       # Test AlternativesTool
```

## 🔒 Production Endpoint (Unchanged)

```
POST /api/v1/query                    # Full pipeline (NL → SLM → Tools)
```

## 🧪 Quick Test

```bash
# Test all endpoints
./test_debug_endpoints.sh

# Or manually
curl -X POST http://localhost:5000/api/v1/debug/search \
  -H "Api-Key: decathlon_smart_search_2024_secure_key_abc123xyz" \
  -H "Content-Type: application/json" \
  -d '{"sport":"Running","keywords":["shoes"],"price_limit":5000}'
```

## 📖 Swagger UI

All endpoints documented:
```
http://localhost:5000/docs
```

## 🎯 Use Cases

| Endpoint | Use Case |
|----------|----------|
| `/debug/parse-query` | Test SLM without database |
| `/debug/search` | Test HybridSearch, SQL, pgvector |
| `/debug/task` | Test budget optimizer, item generation |
| `/debug/compare` | Test product retrieval by ID |
| `/debug/alternatives` | Test similarity logic |

## 📊 Architecture

### Before (v3.0.0):
```
POST /api/v1/query  →  [full pipeline]
```

### After (v3.1.0):
```
POST /api/v1/query                 →  [full pipeline] (unchanged)
POST /api/v1/debug/parse-query     →  [SLM only]
POST /api/v1/debug/search          →  [SearchTool only]
POST /api/v1/debug/task            →  [TaskTool only]
POST /api/v1/debug/compare         →  [CompareTool only]
POST /api/v1/debug/alternatives    →  [AlternativesTool only]
```

## ✅ What Was Preserved

- ✅ Production endpoint `/api/v1/query` unchanged
- ✅ LocalModelClient class unchanged
- ✅ All tools unchanged
- ✅ Authentication unchanged
- ✅ Database layer unchanged
- ✅ Architecture unchanged

## 📝 Files Changed

Modified:
- `backend/api_swagger.py` (added debug endpoints)

Created:
- `test_debug_endpoints.sh` (test script)
- `DEBUG_ENDPOINTS.txt` (full documentation)
- `DEBUG_ENDPOINTS_SUMMARY.txt` (this file)

## 🔑 Authentication

All endpoints require API key:
```
Api-Key: decathlon_smart_search_2024_secure_key_abc123xyz
```

## 🚀 Start Using

1. Start system:
   ```bash
   ./start_system.sh
   ```

2. Open Swagger:
   ```
   http://localhost:5000/docs
   ```

3. Try debug endpoints:
   - Test SLM output
   - Test tools directly
   - Compare with production endpoint

## 📚 Documentation

Read `DEBUG_ENDPOINTS.txt` for:
- Detailed endpoint specs
- Request/response examples
- Use cases
- Testing strategies
- Error handling
